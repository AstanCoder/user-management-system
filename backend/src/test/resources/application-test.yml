spring:
  datasource:
    url: jdbc:h2:mem:contact_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: false
  mail:
    host: localhost
    port: 2525
    username: test
    password: test

app:
  cors:
    allowed-origins: http://localhost:3000
  jwt:
    secret: test-jwt-secret-key-at-least-32-chars-long
    expiration-hours: 1
  mail:
    from: test@nexuscrm.com
  avatar:
    storage-path: ./build/test-avatars
